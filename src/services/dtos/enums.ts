export enum Gender {
    Male = 0,
    Female = 1
}

export enum ExpenseCategory {
    Utilities,
    Equipment,
    Office,
    Staff
}

export enum TreatmentPhotoType {
    Before,
    After
}

export enum DebtOperationType {
    DebtIncurrence,
    DebtPayment
}